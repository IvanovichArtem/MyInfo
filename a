geo_stats = X_train.groupby("minsk_geo_cluster_price")[TARGET].agg(["mean", "median", "min", "max", "std"])
X_train = X_train.merge(geo_stats, on="minsk_geo_cluster_price", suffixes=("", "_geo"))
X_valid = X_valid.merge(geo_stats, on="minsk_geo_cluster_price", suffixes=("", "_geo"))
